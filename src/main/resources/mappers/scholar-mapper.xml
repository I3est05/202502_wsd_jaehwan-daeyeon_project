<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.donjoogga.mapper.ScholarshipMapper">

    <select id="selectAllAdminScholarships" resultType="com.example.donjoogga.vo.Scholarship">
        SELECT
            S.scholar_id        AS refId,
            S.title,
            S.organization,
            S.deadline,
            S.category,
            S.support_amount    AS supportAmount,
            'DB'                AS sourceType
        FROM
            Scholar S
        ORDER BY
            S.deadline ASC
    </select>

    <select id="selectScholarshipById" resultType="com.example.donjoogga.vo.Scholarship">
        SELECT
            S.scholar_id        AS refId,
            S.title,
            S.organization,
            S.deadline,
            S.category,
            S.support_amount    AS supportAmount,
            'DB'                AS sourceType
        FROM
            Scholar S
        WHERE
            S.scholar_id = #{id}
    </select>

</mapper>