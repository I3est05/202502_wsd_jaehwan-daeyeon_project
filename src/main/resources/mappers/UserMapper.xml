<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.donjoogga.mapper.UserMapper">

    <insert id="save" parameterType="com.example.donjoogga.vo.User">
        INSERT INTO User (email, password, income_bracket, gpa, created_at)
        VALUES (#{email}, #{password}, #{incomeBracket}, #{gpa}, NOW())
    </insert>

    <select id="findByEmail" resultType="com.example.donjoogga.vo.User">
        SELECT
            user_id        AS userId,
            email          AS email,
            password       AS password,
            income_bracket AS incomeBracket,
            gpa            AS gpa,
            created_at     AS createdAt
        FROM User
        WHERE email = #{email}
    </select>

</mapper>