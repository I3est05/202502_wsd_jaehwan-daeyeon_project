<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dao.ScholarshipMapper">

    <resultMap id="scholarResult" type="com.example.bean.ScholarVO">
        <id property="id" column="ID"/>
        <result property="title" column="title"/>
        <result property="author" column="author"/>
        <result property="publisher" column="publisher"/>
        <result property="published_date" column="published_date"/>
        <result property="genre" column="genre"/>
        <result property="cnt" column="cnt"/>
    </resultMap>

    <select id="selectAllAdminScholarships" resultType="com.example.ScholarshipDTO">
        SELECT
            S.scholar_id        AS refId,
            S.title,
            S.organization,
            S.deadline,
            S.category,
            S.support_amount,
            S.img,
            'DB'                AS sourceType
        FROM
            Scholar S
        ORDER BY
            S.deadline ASC
    </select>

</mapper>